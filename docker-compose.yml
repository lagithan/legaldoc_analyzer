version: "3.9"

services:
  frontend:
    build: ./legaldoc_frontend
    ports:
      - "8080:3000"   # host:container (local only)
    environment:
      # Not required on Railway unless overriding
      # API_URL: ${API_URL}
    depends_on:
      - backend

  backend:
    build: ./legaldoc_backend
    ports:
      - "8000:8000"   # host:container (local only)
    environment:
      # Not required on Railway unless overriding
      # DB_URL: ${DB_URL}
